import { __exports as metadata_reader } from '../../../../_virtual/metadata_reader.js';
export { __exports as default } from '../../../../_virtual/metadata_reader.js';
import '../constants/metadata_keys.js';
import { __exports as metadata_keys } from '../../../../_virtual/metadata_keys.js';

Object.defineProperty(metadata_reader, "__esModule", { value: true });
metadata_reader.MetadataReader = void 0;
var METADATA_KEY = metadata_keys;
var MetadataReader = (function () {
    function MetadataReader() {
    }
    MetadataReader.prototype.getConstructorMetadata = function (constructorFunc) {
        var compilerGeneratedMetadata = Reflect.getMetadata(METADATA_KEY.PARAM_TYPES, constructorFunc);
        var userGeneratedMetadata = Reflect.getMetadata(METADATA_KEY.TAGGED, constructorFunc);
        return {
            compilerGeneratedMetadata: compilerGeneratedMetadata,
            userGeneratedMetadata: userGeneratedMetadata || {}
        };
    };
    MetadataReader.prototype.getPropertiesMetadata = function (constructorFunc) {
        var userGeneratedMetadata = Reflect.getMetadata(METADATA_KEY.TAGGED_PROP, constructorFunc) || [];
        return userGeneratedMetadata;
    };
    return MetadataReader;
}());
metadata_reader.MetadataReader = MetadataReader;
