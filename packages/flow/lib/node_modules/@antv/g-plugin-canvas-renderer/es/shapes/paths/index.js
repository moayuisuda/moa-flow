import { Syringe } from 'mana-syringe';

var PathGeneratorFactory = Syringe.defineToken('PathGeneratorFactory');
var PathGenerator = Syringe.defineToken('PathGenerator');

export { PathGenerator, PathGeneratorFactory };
