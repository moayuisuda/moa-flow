import '../../../_@antv_g@5.0.22@@antv/g/es/index.js';
import { Shape } from '../../../_@antv_g@5.0.22@@antv/g/es/types.js';

function ElementOf(type) {
  return type;
}
var Circle = ElementOf(Shape.CIRCLE);
ElementOf(Shape.ELLIPSE);
var Group = ElementOf(Shape.GROUP);
ElementOf(Shape.HTML);
ElementOf(Shape.IMAGE);
ElementOf(Shape.LINE);
ElementOf(Shape.PATH);
ElementOf(Shape.POLYGON);
var Polyline = ElementOf(Shape.POLYLINE);
var Rect = ElementOf(Shape.RECT);
var Text = ElementOf(Shape.TEXT);

export { Circle, ElementOf, Group, Polyline, Rect, Text };
