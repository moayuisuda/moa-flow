import { Shape } from '../types.js';
import '../display-objects/DisplayObject.js';
import '../display-objects/CustomElement.js';
import { Circle } from '../display-objects/Circle.js';
import { Ellipse } from '../display-objects/Ellipse.js';
import { Group } from '../display-objects/Group.js';
import { Image } from '../display-objects/Image.js';
import { Line } from '../display-objects/Line.js';
import { Path } from '../display-objects/Path.js';
import { Polygon } from '../display-objects/Polygon.js';
import { Polyline } from '../display-objects/Polyline.js';
import { Rect } from '../display-objects/Rect.js';
import { Text } from '../display-objects/Text.js';
import { HTML } from '../display-objects/HTML.js';

/**
 * canvas.customElements
 *
 * @see https://developer.mozilla.org/en-US/docs/Web/API/CustomElementRegistry
 */

var CustomElementRegistry =
/** @class */
function () {
  function CustomElementRegistry() {
    this.registry = {};
    this.define(Shape.CIRCLE, Circle);
    this.define(Shape.ELLIPSE, Ellipse);
    this.define(Shape.RECT, Rect);
    this.define(Shape.IMAGE, Image);
    this.define(Shape.LINE, Line);
    this.define(Shape.GROUP, Group);
    this.define(Shape.PATH, Path);
    this.define(Shape.POLYGON, Polygon);
    this.define(Shape.POLYLINE, Polyline);
    this.define(Shape.TEXT, Text);
    this.define(Shape.HTML, HTML);
  }

  CustomElementRegistry.prototype.define = function (name, constructor) {
    this.registry[name] = constructor;
  };
  /**
   * @see https://developer.mozilla.org/en-US/docs/Web/API/CustomElementRegistry/get
   */


  CustomElementRegistry.prototype.get = function (name) {
    return this.registry[name];
  };

  return CustomElementRegistry;
}();

export { CustomElementRegistry };
