import { __extends } from '../../../../../_tslib@2.4.0@tslib/tslib.es6.js';
import { CSSStyleValueType, CSSStyleValue } from './CSSStyleValue.js';

/**
 * represents transform-list values as used by the CSS transform property.
 *
 * @see https://developer.mozilla.org/en-US/docs/Web/API/CSSTransformValue
 */

/** @class */
(function (_super) {
  __extends(CSSTransformValue, _super);

  function CSSTransformValue(transforms) {
    var _this = _super.call(this) || this;

    _this.transforms = transforms;
    _this.is2D = transforms.every(function (transform) {
      return transform.is2D;
    });
    _this.length = transforms.length;
    return _this;
  }

  CSSTransformValue.prototype.clone = function () {
    return new CSSTransformValue(this.transforms);
  };

  CSSTransformValue.prototype.getType = function () {
    return CSSStyleValueType.kTransformType;
  };

  CSSTransformValue.prototype.buildCSSText = function (n, p, result) {
    var text = '';
    return result += text;
  };
  /**
   * returns a DOMMatrix object
   * @see https://developer.mozilla.org/en-US/docs/Web/API/CSSTransformValue/toMatrix
   */


  CSSTransformValue.prototype.toMatrix = function () {};

  return CSSTransformValue;
})(CSSStyleValue);
