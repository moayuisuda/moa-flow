import React from "react";
import { Button, Space } from "antd";

const Toolbar = (model) => {
  return (
    <Space>
      <Button>dele</Button>
      <Button>show meta</Button>
    </Space>
  );
};

export default Toolbar;
