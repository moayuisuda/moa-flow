// @ts-nocheck

        // @ts-ignore
        import * as ctoken from './alias';

        const { getToken } = ctoken;

        export {
          ctoken,
          getToken,
        }

        export default ctoken;
      